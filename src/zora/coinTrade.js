// coinTrade.js
// Functions to trade (transfer) coins with Zora SDK

export async function tradeCoin({ recipient, amount, tokenAddress, signer }) {
  // TODO: Use @zoralabs/coins-sdk to transfer tokens
  console.log('Trade coin:', { recipient, amount, tokenAddress, signer });
  // Example: await coinsSDK.transfer(...)
} 